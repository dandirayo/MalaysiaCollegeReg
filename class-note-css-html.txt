
(rev)
(so-so)

---INDEX.HTML---
index container (so-so)
	>> navigation-container(rev)
		> logo-container
		> menu-container
			> ul-menu
				> li-menu
					> a-menu
			> ul-menu
				> li-menu
					> a-menu
			> ul-menu
				> li-menu
					> a-menu
		> search-container
			> bar-search
			> button-search(rev)
		> login-button
	>> img-container(50)
		> col-image
	>> footer-container(rev)
		> p-footer

---COLLEGE.HTML---
college-container
	>> navigation-container(rev)
		> logo-container
		> menu-container
			> ul-menu
				> li-menu
					> a-menu
			> ul-menu
				> li-menu
					> a-menu
			> ul-menu
				> li-menu
					> a-menu
		> search-container
			> bar-search
			> button-search(rev)
		> login-button	
	>> col-content
		> first-college
		> second-college
	>> footer-container(rev)
		> p-footer
		
---ABOUT.HTML---
about-container
	>> navigation-container(rev)
		> logo-container
		> menu-container
			> ul-menu
				> li-menu
					> a-menu
			> ul-menu
				> li-menu
					> a-menu
			> ul-menu
				> li-menu
					> a-menu
		> search-container
			> bar-search 
			> button-search(rev)
		> login-button	
	>> con-container
		> about-image
		> about-text
		> about-socmed
		> about-thanks
	>> footer-container (rev)
		> p-footer
	
		
		

		
		